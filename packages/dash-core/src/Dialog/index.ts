import { Dialog as dialog, DialogTrigger as dialogTrigger, DialogClose as dClose } from "./Dialog.styles";
import DialogContentComp from "./DialogContent";


export const  Dialog = dialog;
export const DialogTrigger = dialogTrigger;
 export const DialogContent = DialogContentComp
 export const DialogClose = dClose