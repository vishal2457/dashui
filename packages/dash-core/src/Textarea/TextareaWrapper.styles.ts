import { cssFocusVisible, styled } from "../theme/stitches.config";


export const StyledTextareaWrapper= styled(
    'div', {
        textAlign: 'left',
        marginBottom: 15,
    },
    cssFocusVisible
  );
  